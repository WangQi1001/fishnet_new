import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fishnet_app_new',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
